all: DOMINION RESETDOM

PFLAGS=-g -vb

RESETDOM: RESETDOM.PAS
	fpc ${PFLAGS} RESETDOM.PAS

DOMINION: DOMINION.PAS COMMON.PAS OVERLAY1.DOM OVERLAY2.DOM OVERLAY3.DOM
	fpc ${PFLAGS} DOMINION.PAS

test: test.pas
	fpc ${PFLAGS} test.pas

clean:
	rm -fv DOMINION RESETDOM test *.o
